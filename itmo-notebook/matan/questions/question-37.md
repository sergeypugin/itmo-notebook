---
title: Свойства равномерно сходящихся рядов
author: Romchik
links:
  - "[Бойцев база.pdf](https://drive.google.com/file/d/1fd1CA2uUsD9ZnaquCyoEYYzXWVJyVRVN/view?usp=drivesdk) гл. 9, § 3"
---
### 1. Почленный переход к пределу

>***Предельная точка*** - это точка, в любой окрестности которой есть соседняя (эквивалентно тому, что их бесконечно много)

Пусть задан функциональный ряд $\sum_{k=1}^\infty f_k(x)$, где $f_k: D \to \mathbb{R}$, и $x_0$ — предельная точка множества $D$. Если выполнены два условия:
1. Ряд равномерно сходится на $D$ к сумме $S(x)$.
2. Для каждого $k \in \mathbb{N}$ существует конечный предел $\lim_{x \to x_0} f_k(x) = a_k$.

Тогда числовой ряд $\sum_{k=1}^\infty a_k$ сходится к некоторой сумме $A$, причем предел суммы функционального ряда в точке $x_0$ равен сумме пределов его членов:
$$
\lim_{x \to x_0} S(x) = \lim_{x \to x_0} \sum_{k=1}^\infty f_k(x) = \sum_{k=1}^\infty \lim_{x \to x_0} f_k(x) = A
$$

#### Доказательство

Сводится к применению теоремы о перестановке двух предельных переходов для функциональной последовательности частичных сумм $S_n(x) = \sum_{k=1}^n f_k(x)$. Для $S_n(x)$ мы имеем равномерную сходимость к $S(x)$ на $D$ и существование пределов $\lim_{x \to x_0} S_n(x) = \sum_{k=1}^n a_k$. Следовательно, по теореме для последовательностей, пределы $\lim_{n \to \infty} \lim_{x \to x_0} S_n(x)$ и $\lim_{x \to x_0} \lim_{n \to \infty} S_n(x)$ существуют и равны друг другу,.

### 2. Непрерывность суммы ряда

Пусть $f_k: D \to \mathbb{R}$, $x_0 \in D$. Если:
1. Ряд $\sum_{k=1}^\infty f_k(x)$ равномерно сходится на $D$ к сумме $S(x)$.
2. Все члены ряда $f_k(x)$ непрерывны в точке $x_0$.

Тогда сумма ряда $S(x)$ также непрерывна в точке $x_0$. 
*(В частности, если все $f_k$ непрерывны на всем $D$, то и сумма $S$ непрерывна на $D$)*.

#### Доказательство:

Рассмотрим $n$-ю частичную сумму $S_n(x) = \sum_{k=1}^n f_k(x)$. Как конечная сумма непрерывных в точке $x_0$ функций, $S_n(x)$ непрерывна в $x_0$.

По условию $S_n(x) \rightrightarrows S(x)$ на $D$. Тогда, используя возможность перестановки пределов, получаем:
	$$
	\lim_{x \to x_0} S(x) = \lim_{x \to x_0} \lim_{n \to \infty} S_n(x) = \lim_{n \to \infty} \lim_{x \to x_0} S_n(x) = \lim_{n \to \infty} S_n(x_0) = S(x_0)
	$$
Равенство $\lim_{x \to x_0} S(x) = S(x_0)$ по определению означает непрерывность суммы ряда в точке $x_0$,.

>[!important] Последующие свойства не являются чем-то сверхъестественным, так что учитывая размеры этого вопроса, расписывать их вряд ли понадобится.

### 3. Почленное интегрирование ряда


Пусть функции $f_k: [a, b] \to \mathbb{R}$ непрерывны на отрезке $[a, b]$ ($f_k \in C[a, b]$). Если функциональный ряд $\sum_{k=1}^\infty f_k(x)$ сходится равномерно к функции $S(x)$ на $[a, b]$, то сумма ряда $S(x)$ непрерывна (и следовательно, интегрируема) на $[a, b]$, причем ряд можно интегрировать почленно:
$$
\int_a^x \left( \sum_{k=1}^\infty f_k(t) \right) dt = \sum_{k=1}^\infty \int_a^x f_k(t) dt, \quad x \in [a, b]
$$

#### Доказательство:

Зададим $\varepsilon > 0$. В силу равномерной сходимости $S_n(x) \rightrightarrows S(x)$, существует номер $k_0$, такой что для всех $n > k_0$ и для всех $t \in [a, b]$ выполняется неравенство:
	$$
	|S(t) - S_n(t)| < \frac{\varepsilon}{b - a}
	$$
Тогда для любого $x \in [a, b]$ оценим разность интегралов:
	$$
	\left| \int_a^x S_n(t) dt - \int_a^x S(t) dt \right| \le \int_a^x |S_n(t) - S(t)| dt \le \frac{\varepsilon}{b - a} (x - a) \le \varepsilon
	$$
Это означает, что последовательность интегралов от частичных сумм равномерно сходится к интегралу от суммы ряда, что равносильно утверждению теоремы,.

### 4. Почленное дифференцирование ряда

Пусть функции $f_k: [a, b] \to \mathbb{R}$ непрерывно дифференцируемы на $[a, b]$ ($f_k \in C^1[a, b]$). Если выполнены два условия:
1. Существует хотя бы одна точка $x_0 \in [a, b]$, в которой числовой ряд $\sum_{k=1}^\infty f_k(x_0)$ сходится.
2. Ряд из производных $\sum_{k=1}^\infty f_k'(x)$ сходится на $[a, b]$ равномерно к некоторой сумме $\tilde{S}(x)$.

Тогда исходный ряд $\sum_{k=1}^\infty f_k(x)$ сходится на $[a, b]$ равномерно к некоторой сумме $S(x)$, причем $S'(x) = \tilde{S}(x)$ на всем $[a, b]$ (то есть ряд можно дифференцировать почленно). В частности, $S \in C^1[a, b]$.

#### Доказательство:
По условию 2, последовательность $S'_n(x)$ сходится равномерно к $\tilde{S}(x)$, а все $S_n'(x)$ непрерывны. Следовательно, $\tilde{S}(x)$ также непрерывна.
По теореме о почленном интегрировании, равномерно сходящуюся последовательность $S_n'(t)$ можно проинтегрировать от $x_0$ до $x$:
	$$
	\lim_{n \to \infty} \int_{x_0}^x S_n'(t) dt = \int_{x_0}^x \tilde{S}(t) dt
	$$
По формуле Ньютона-Лейбница $\int_{x_0}^x S_n'(t) dt = S_n(x) - S_n(x_0)$. Отсюда:
	$$
	\lim_{n \to \infty} (S_n(x) - S_n(x_0)) = \int_{x_0}^x \tilde{S}(t) dt
	$$
По условию 1, предел $\lim_{n \to \infty} S_n(x_0)$ существует (обозначим его за $C$). Тогда существует и равномерный предел последовательности $S_n(x)$:
	$$
	S(x) = \lim_{n \to \infty} S_n(x) = C + \int_{x_0}^x \tilde{S}(t) dt
	$$
По теореме о производной интеграла по верхнему пределу (так как $\tilde{S}(t)$ непрерывна), дифференцирование обеих частей дает:
	$$
	S'(x) = \tilde{S}(x)
	$$
Что и доказывает справедливость почленного дифференцирования ряда.
